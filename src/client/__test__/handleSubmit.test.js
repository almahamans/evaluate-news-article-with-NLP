import {handleSubmit} from '../js/formHandler'

describe('Testing the submit function',()=>{
    test('Testing the handleSubmit() functio', () => { 
        expect(handleSubmit).toBeDefined()
     })
})