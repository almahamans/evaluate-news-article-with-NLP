import {checkForName} from '../js/nameChecker'

describe('Testing the names function',()=>{
    test('Testing the checkForName() functio', () => { 
        expect(checkForName).toBeDefined()
     })
})